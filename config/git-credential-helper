#!/bin/sh
# Git credential helper for GitHub token authentication.
# Returns the token from GITHUB_PERSONAL_ACCESS_TOKEN env var.
# The token is never written to disk — read from environment at invocation time.
printf 'username=x-access-token\npassword=%s\n' "$GITHUB_PERSONAL_ACCESS_TOKEN"
